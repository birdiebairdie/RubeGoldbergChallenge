using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TeleportTargetObject: MonoBehaviour {
	//needs to be 2 paired objects (like portal)
	//different colors
	//when entering one object, the ball needs to be moved to the second object
	//	and the velocity and angular velocity need to be adjusted to fit the direction the second object is facing



	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
